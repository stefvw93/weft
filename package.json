{
  "name": "weft-workspace",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "vp config"
  },
  "devDependencies": {
    "@effect/language-service": "^0.86.4",
    "@semantic-release/commit-analyzer": "^13.0.1",
    "@semantic-release/exec": "^7.0.0",
    "@semantic-release/github": "^12.0.8",
    "@semantic-release/release-notes-generator": "^14.1.1",
    "@vitest/browser-playwright": "catalog:",
    "playwright": "^1.61.1",
    "semantic-release": "^25.0.5",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vite-plus": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "node": ">=26.2.0 <27",
    "pnpm": ">=11.1.3 <12"
  },
  "engineStrict": true,
  "packageManager": "pnpm@11.1.3",
  "pnpm": {
    "overrides": {
      "//": "Dedupe effect to a single instance — @effect-atom's subtree otherwise pulls 3.21.3 while the workspace catalog uses ^3.21.4, splitting effect's branded types across two copies and breaking cross-package type-checks. A range (not an exact pin) keeps patch/minor bumps open.",
      "effect": "^3.21.4"
    }
  }
}
